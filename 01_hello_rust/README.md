# rust hello world

这是一个简单的 Rust 程序，打印 "Hello, world!" 到控制台

```rust
fn main() {
    println!("Hello, world!");
}
```

- `fn main()` 定义了程序的入口点
- `println!` 是一个宏，用于打印输出到控制台

运行这个程序时，你会看到以下输出：

```
Hello, world!
```
